apiVersion: v1
kind: Service
metadata:
  name: {{ include "gorse.fullname" . }}
  labels:
    {{- include "gorse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gorse.selectorLabels" . | nindent 4 }}
